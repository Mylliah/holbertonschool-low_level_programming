#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int alpha;

	for (alpha = 0 ; alpha < 10 ; alpha++)
	{
		printf("%d", alpha);
	}
	printf("\n");
	return (0);
}
